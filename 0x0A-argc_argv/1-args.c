#include <stdio.h>

/**
 * main - print number of arguments passed into the string
 * @argc: arguments counts
 * @argv: list of array pointing to the string
 * Return: Return void
 */
int main(int argc, char *argv[])
{
	argv = argv;
	printf("%d\n", argc - 1);
	return (0);
}
