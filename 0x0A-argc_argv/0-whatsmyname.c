#include <stdio.h>

/**
 * main - function that prints its name
 * @argc: arguments counts
 * @argv: list of array pointing to string
 * Return: Return void
 */
int main(int argc, char *argv[])
{
	argc = argc;
	printf("%s\n", *argv);
	return (0);
}
