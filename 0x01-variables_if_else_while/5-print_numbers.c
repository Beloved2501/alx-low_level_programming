#include <stdio.h>

/**
 * main - Entry point of all program
 *
 * Return: Always in 0
 */
int main(void)
{
	int number;

	for (number = 0; number < 10; number++)
	{
		printf(number = '0');
	}

	putchar('\n');

	return (0);
}
